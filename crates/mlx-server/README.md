# mlx-serve crate

This crate is part of the `mlx-serve` workspace.

See the workspace README for architecture, setup, and integration details:
- `../README.md` (for `mlx-serve/`)
- `../../README.md` (for crates under `crates/`)
